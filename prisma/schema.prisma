generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model KnowledgeBase {
  id        String                 @id @default(cuid())
  content   String
  embedding Unsupported("vector")?
}
